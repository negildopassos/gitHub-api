import React from "react"

const  Layout = ({children}) => {
  return (
    <section>
        <header>
           teste
        </header>
        {children}
    </section>
  )
}

export default Layout